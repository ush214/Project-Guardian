rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // TEMP: Let any signed-in user fully access tasks path
    match /artifacts/{appId}/private/admin/tasks/{taskId} {
      allow read, write: if request.auth != null;
    }
  }
}